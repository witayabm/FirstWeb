services:
  web:
    build: .
    image: sp500-stock-dashboard:latest
    ports:
      - "3000:3000"
    environment:
      FMP_API_KEY: ${FMP_API_KEY:-}
      PORT: 3000
      BACKEND_PORT: 3001
      API_BASE_URL: ""
      API_PROXY_URL: http://127.0.0.1:3001
    restart: unless-stopped
